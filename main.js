const userName = prompt("ismingizni kirting")
const userAge = +prompt("space idingizni kirting")
const userMarride  = +prompt("space parolingizni kirting")
const oquvchilar = {
    ismi : userName , 
    id : userAge,
    password : userMarride,
}
console.log(oquvchilar);
